# 后台编辑器设计文档（基于左右布局）

> **注意**：后台预览采用与前台展示统一的设计系统。查看完整的设计规范：[DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md)

## 页面布局

采用与前台一致的左右结构布局：

```
┌─────────────────────────────────────────────────────────┐
│  编辑版本 v1.0.0                          [保存][取消]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  基本信息                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 版本号 *        [1.0.0                      ]   │   │
│  │ 产品 *          [Jetbrains ▼                ]   │   │
│  │ 状态            ✅ 已发布 / 📝 草稿           │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  发布内容（左右布局预览）                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ┌──────────────┐  ┌──────────────────────────┐  │   │
│  │ │  左侧预览     │  │  右侧编辑区             │  │   │
│  │ │              │  │                          │  │   │
│  │ │ 2026年01月   │  │  标题 *                  │  │   │
│  │ │ JetBrains    │  │  [新增数据库关联...      ]  │   │
│  │ │ 1.0.0        │  │                          │  │   │
│  │ │              │  │  更新内容                │  │   │
│  │ │              │  │  ┌─────────────────────┐  │  │   │
│  │ │              │  │  │ 特性         [删除] │  │  │   │
│  │ │              │  │  │ ─────────────────── │  │  │   │
│  │ │              │  │  │ 全面支持数据库       │  │  │   │
│  │ │              │  │  │ [编辑]               │  │  │   │
│  │ │              │  │  │                     │  │  │   │
│  │ │              │  │  │ 新增自定义指令       │  │  │   │
│  │ │              │  │  │ [编辑]               │  │  │   │
│  │ │              │  │  │ [+ 添加条目]         │  │  │   │
│  │ │              │  │  └─────────────────────┘  │  │   │
│  │ │              │  │                          │  │   │
│  │ │              │  │  [+ 添加分类]             │  │   │
│  │ │              │  │                          │  │  │   │
│  │ │              │  │                          │  │   │
│  │ │              │  │                          │  │   │
│  │ │              │  │                          │  │   │
│  │ │              │  │  附件（可选）             │  │   │
│  │ │              │  │  [+ 上传图片/视频]        │  │   │
│  │ │              │  │                          │  │   │
│  │ └──────────────┘  └──────────────────────────┘  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [保存草稿]  [预览]  [发布并构建]                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 编辑流程

### 步骤 1: 填写基本信息

```
版本号: 1.0.0
产品:   JetBrains (选择 IDE/JetBrains/CLI)
状态:   📝 草稿 / ✅ 已发布
```

### 步骤 2: 输入标题

```
标题: 新增数据库关联和指令能力
```

### 步骤 3: 添加更新内容

点击 **[+ 添加分类]**，选择类型：
- **特性** - 新功能
- **优化** - 改进
- **修复** - Bug 修复

然后添加内容条目：

```
特性
┌─────────────────────────────────────┐
│ 全面支持数据库           [编辑][删除]│
│ 支持在Qoder的Ask/Agent模式...[编辑][删除]│
│ 支持在Query Console中生成...[编辑][删除]│
│ [+ 添加条目]                        │
└─────────────────────────────────────┘
```

### 步骤 4: 预览效果

左侧实时预览显示前台效果：

```
┌──────────────┐
│ 2026年01月   │
│ JetBrains    │
│ 1.0.0        │
└──────────────┘
```

```
┌──────────────────────────────────┐
│ 新增数据库关联和指令能力          │
│                                   │
│ 特性                              │
│ • 全面支持数据库                   │
│   支持在Qoder的Ask/Agent模式下...  │
│   支持在Query Console中生成...     │
│ • 新增自定义指令                   │
│   将常用提示词或工作流封装...       │
└──────────────────────────────────┘
```

---

## 数据结构

### 提交到服务器的数据

```json
{
  "version": "1.0.0",
  "product": "JetBrains",
  "title": "新增数据库关联和指令能力",
  "status": "published",
  "updates": [
    {
      "id": "1",
      "category": "feature",
      "items": [
        "全面支持数据库",
        "支持在Qoder的Ask/Agent模式下引用数据库作为上下文",
        "支持在Query Console中生成SQL并执行"
      ]
    },
    {
      "id": "2",
      "category": "feature",
      "items": [
        "新增自定义指令",
        "将常用提示词或工作流封装为指令，在 Agent 中通过 / 一键调用，提升日常开发效率",
        "PlantUML渲染能力升级",
        "允许通过工具编辑工程外的文件"
      ]
    },
    {
      "id": "3",
      "category": "improvement",
      "items": [
        "优化上下文 @gitcommit等问题"
      ]
    }
  ]
}
```

---

## 关键特性

### 1. 简单的内容输入

- ✅ 每个分类下直接输入文本
- ✅ 一行一条内容
- ✅ 自动添加列表符号
- ✅ 支持添加/删除/编辑

### 2. 左侧实时预览

- 显示日期和版本号
- 与前台展示保持一致
- 无需切换页面

### 3. 右侧编辑区

- 简洁的输入框
- 支持添加多个分类
- 支持拖拽排序（可选）

---

## 技术实现

### 客户端组件

```typescript
'use client';

import { useState } from 'react';

export default function VersionEditor() {
  const [formData, setFormData] = useState({
    version: '',
    product: 'JetBrains' as ProductType,
    title: '',
    status: 'draft' as 'draft' | 'published',
    updates: [
      {
        id: '1',
        category: 'feature' as const,
        items: ['']
      }
    ]
  });

  // 添加分类
  function addCategory() {
    setFormData({
      ...formData,
      updates: [
        ...formData.updates,
        {
          id: Date.now().toString(),
          category: 'feature',
          items: ['']
        }
      ]
    });
  }

  // 添加条目
  function addItem(updateId: string) {
    setFormData({
      ...formData,
      updates: formData.updates.map(update =>
        update.id === updateId
          ? { ...update, items: [...update.items, ''] }
          : update
      )
    });
  }

  // 更新条目内容
  function updateItem(updateId: string, itemIndex: number, value: string) {
    setFormData({
      ...formData,
      updates: formData.updates.map(update => {
        if (update.id === updateId) {
          const newItems = [...update.items];
          newItems[itemIndex] = value;
          return { ...update, items: newItems };
        }
        return update;
      })
    });
  }

  // 删除条目
  function removeItem(updateId: string, itemIndex: number) {
    setFormData({
      ...formData,
      updates: formData.updates.map(update => {
        if (update.id === updateId) {
          return {
            ...update,
            items: update.items.filter((_, i) => i !== itemIndex)
          };
        }
        return update;
      })
    });
  }

  // ...
}
```

### 编辑区组件

```typescript
function EditSection({
  updates,
  onUpdate,
  onAddItem,
  onRemoveItem,
  onAddCategory
}: EditSectionProps) {
  return (
    <div className="space-y-4">
      {updates.map((update) => (
        <div key={update.id} className="border rounded-lg p-4">
          {/* 分类选择 */}
          <select
            value={update.category}
            onChange={(e) => onUpdate(update.id, 'category', e.target.value)}
            className="mb-3 block w-full rounded border-gray-300"
          >
            <option value="feature">特性</option>
            <option value="improvement">优化</option>
            <option value="fix">修复</option>
          </select>

          {/* 内容列表 */}
          <div className="space-y-2">
            {update.items.map((item, index) => (
              <div key={index} className="flex gap-2">
                <input
                  type="text"
                  value={item}
                  onChange={(e) => onUpdate(update.id, index, e.target.value)}
                  placeholder="输入更新内容..."
                  className="flex-1 rounded border-gray-300"
                />
                <button
                  onClick={() => onRemoveItem(update.id, index)}
                  className="px-3 py-1 text-red-600 hover:bg-red-50 rounded"
                >
                  删除
                </button>
              </div>
            ))}
          </div>

          {/* 添加条目按钮 */}
          <button
            onClick={() => onAddItem(update.id)}
            className="mt-2 text-sm text-blue-600 hover:underline"
          >
            + 添加条目
          </button>
        </div>
      ))}

      {/* 添加分类按钮 */}
      <button
        onClick={onAddCategory}
        className="w-full py-2 border-2 border-dashed border-gray-300 rounded text-gray-600 hover:border-blue-400 hover:text-blue-600"
      >
        + 添加分类
      </button>
    </div>
  );
}
```

---

## 前台展示效果

```
┌─────────────────────────────────────────────────┐
│  2026年01月      新增数据库关联和指令能力          │
│  JetBrains                                        │
│  1.0.0                                            │
│                                                   │
│  特性                                              │
│  • 全面支持数据库                                  │
│    支持在Qoder的Ask/Agent模式下引用数据库作为上下文│
│    支持在Query Console中生成SQL并执行            │
│  • 新增自定义指令                                  │
│    将常用提示词或工作流封装为指令，在 Agent 中... │
│    PlantUML渲染能力升级                           │
│    允许通过工具编辑工程外的文件                     │
│                                                   │
│  优化                                              │
│  • 优化上下文 @gitcommit等问题                      │
└─────────────────────────────────────────────────┘
```

---

## 响应式适配

### 桌面端 (>768px)

- 左侧：固定宽度 128px
- 右侧：自适应宽度
- 左右布局

### 移动端 (<768px)

- 上下布局
- 日期和版本在上方
- 内容在下方

```css
/* 桌面端 */
@media (min-width: 768px) {
  article {
    display: flex;
    gap: 2rem;
  }

  aside {
    width: 128px;
    flex-shrink: 0;
  }
}

/* 移动端 */
@media (max-width: 767px) {
  article {
    display: block;
  }

  aside {
    margin-bottom: 1rem;
  }
}
```

---

## 总结

### 核心优势

1. ✅ **极简编辑** - 只需输入文本，无需富文本编辑
2. ✅ **实时预览** - 左侧预览，右侧编辑
3. ✅ **一致性** - 编辑器与前台展示保持一致
4. ✅ **响应式** - 自动适配桌面和移动设备

### 与参考网站的对比

| 特性 | 参考网站 | 本系统 |
|------|---------|--------|
| 左右布局 | ✅ | ✅ |
| 简洁内容 | ✅ | ✅ |
| 分类展示 | ✅ | ✅ |
| 实时预览 | ❌ | ✅ |
| 后台编辑 | ❌ | ✅ |

这个设计完美复制了参考网站的风格，同时添加了后台编辑功能。
