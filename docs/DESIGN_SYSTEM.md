# 设计系统规范

本文档定义了 Changelog 系统的完整设计规范，包括字体层级、颜色、间距、布局等。

---

## 字体层级系统

### 前台展示 & 后台预览

系统使用统一的字体层级，确保视觉层次清晰：

| 元素 | Tailwind 类名 | 移动端 | 桌面端 | 字重 | 颜色 |
|------|--------------|--------|--------|------|------|
| **版本标题** (Article Title) | `text-lg lg:text-xl` | 18px | 20px | `font-bold` | `text-gray-900` |
| **分类标题** (特性/优化/修复) | `text-lg lg:text-xl` | 18px | 20px | Normal | `text-gray-800` |
| **一级列表项** | `text-base` | 16px | 16px | Normal | `text-gray-700` |
| **二级列表项** | `text-sm` | 14px | 14px | Normal | `text-gray-600` |

### 导航栏

| 元素 | Tailwind 类名 | 字号 | 字重 |
|------|--------------|------|------|
| 站点标题 | `text-base` | 16px | `font-bold` |
| 站点描述 | `text-xs` | 12px | Normal |
| 产品标签 | `text-xs` | 12px | `font-medium` |

### 移动端特殊布局

在移动端（`< lg`），版本标题采用特殊格式：

```
v1.0.2: 添加图片功能
```

- 版本号：`text-xl font-bold text-purple-600`
- 冒号：`text-gray-400`
- 标题：`text-xl font-bold text-gray-900`
- 发布日期：`text-xs text-gray-500`

---

## 颜色系统

### 主色调

| 用途 | 颜色类 | Hex | 说明 |
|------|--------|-----|------|
| 主渐变（起始） | `from-purple-600` | #9333EA | 紫色 |
| 主渐变（结束） | `to-blue-600` | #2563EB | 蓝色 |
| 主色（纯色） | `purple-600` | #9333EA | 用于标签、图标 |
| 悬停背景 | `bg-purple-50` | #FAF5FF | 浅紫背景 |

### 文本颜色

| 用途 | 颜色类 | Hex |
|------|--------|-----|
| 主标题 | `text-gray-900` | #111827 |
| 副标题 | `text-gray-800` | #1F2937 |
| 正文 | `text-gray-700` | #374151 |
| 次要文本 | `text-gray-600` | #4B5563 |
| 辅助文本 | `text-gray-500` | #6B7280 |
| 占位符 | `text-gray-400` | #9CA3AF |

### 功能色

| 用途 | 颜色类 | Hex |
|------|--------|-----|
| 错误 | `text-red-600` / `bg-red-50` | #DC2622 |
| 成功 | `text-green-600` / `bg-green-50` | #16A34A |
| 信息 | `text-blue-600` / `bg-blue-50` | #2563EB |

---

## 间距系统

### 版本卡片间距

| 位置 | Tailwind 类名 | 移动端 | 桌面端 |
|------|--------------|--------|--------|
| 版本之间 | `space-y-8 lg:space-y-10` | 32px | 40px |
| 版本标题到内容标题 | `mb-4 lg:mb-6` | 16px | 24px |
| 分类之间 | `mb-5 lg:mb-6` | 20px | 24px |
| 分类标题到列表 | `mb-3 lg:mb-4` | 12px | 16px |
| 一级列表项之间 | `space-y-2` | 8px | 8px |
| 二级列表项之间 | `space-y-1.5` | 6px | 6px |

### 列表缩进

| 级别 | Tailwind 类名 | 缩进 |
|------|--------------|------|
| 一级列表 | - | - |
| 二级列表 | `ml-5` | 20px |

### 导航栏间距

| 位置 | Tailwind 类名 | 值 |
|------|--------------|-----|
| 内边距（垂直） | `py-3` | 12px |
| Logo/标题间距 | `gap-2` | 8px |
| 元素间距 | `gap-4` | 16px |
| 标签间距 | `gap-1.5` | 6px |

---

## 布局规范

### 前台展示

#### 桌面端（≥1024px）

```
┌─────────────────────────────────────────────┐
│  左侧边栏 (w-40, 160px)    右侧内容        │
│  ┌──────────────┐         ┌──────────────┐ │
│  │ 日期 (粗体)  │         │ 版本标题     │ │
│  │ 版本号 (标签)│         │              │ │
│  └──────────────┘         │ 分类标题     │ │
│                          │ • 一级列表   │ │
│                          │   二级列表   │ │
│                          └──────────────┘ │
└─────────────────────────────────────────────┘
```

- 使用 `lg:flex-row gap-4 lg:gap-8`
- 左侧固定宽度 `w-40`，`sticky top-24`
- 右侧自适应 `lg:flex-1`

#### 移动端（<1024px）

```
┌─────────────────────────────────────────────┐
│  v1.0.2: 添加图片功能                       │
│  2026年01月19日                             │
│                                             │
│  分类标题                                   │
│  • 一级列表                                 │
│    二级列表                                 │
└─────────────────────────────────────────────┘
```

- 使用 `flex-col`
- 版本标题在前：`v1.0.2: Title`
- 日期在标题下方小字显示

### 后台编辑器

#### 桌面端（≥1024px）

```
┌─────────────────────────────────────────────┐
│  左侧预览 (w-28, 112px)  右侧编辑区        │
│  ┌──────────────┐         ┌──────────────┐ │
│  │ 产品 (标签)  │         │ 版本标题     │ │
│  │ 版本号       │         │              │ │
│  │ 日期         │         │ 更新内容     │ │
│  └──────────────┘         │              │ │
│                          │              │ │
└─────────────────────────────────────────────┘
```

- 左侧预览 `w-28`，固定宽度
- 右侧编辑区 `flex-1`，自适应
- 使用 `grid grid-cols-[112px_1fr] gap-4`

---

## 圆角规范

| 元素 | Tailwind 类名 | 半径 |
|------|--------------|------|
| 按钮 | `rounded-lg` | 8px |
| 输入框 | `rounded-lg` | 8px |
| 卡片 | `rounded-2xl` | 16px |
| 标签（产品/版本） | `rounded-lg` | 8px |
| 标签（圆角按钮） | `rounded-full` | 完全圆角 |
| 图片 | `rounded-lg` | 8px |

---

## 阴影规范

| 用途 | Tailwind 类名 |
|------|--------------|
| 按钮 | `shadow-md` |
| 卡片/模态框 | `shadow-xl` |
| 导航栏 | `shadow-sm` |
| Logo 容器 | `shadow-md` |

---

## 图标与装饰

### 列表图标

| 级别 | 样式 | Tailwind 类名 |
|------|------|--------------|
| 一级列表 | 实心圆点 | `w-1.5 h-1.5 rounded-full bg-purple-600` |
| 二级列表 | 空心圆点 | `w-1.5 h-1.5 rounded-full border-2 border-purple-400` |

### 图标大小

| 用途 | 尺寸 |
|------|------|
| Logo 图标 | `w-7 h-7` (28px) |
| 登录页大图标 | `w-16 h-16` (64px) |
| 空状态图标 | `text-4xl` (36px) |

---

## 动画规范

### 淡入动画

```tsx
className="animate-fade-in"
style={{ animationDelay: `${index * 100}ms` }}
```

每个版本卡片延迟 100ms 依次淡入。

### 过渡效果

| 元素 | Tailwind 类名 | 时长 |
|------|--------------|------|
| 按钮 | `transition-all` | 150ms (默认) |
| 产品标签 | `transition-all duration-200` | 200ms |
| 图片悬停 | `transition-opacity` | 150ms (默认) |

---

## 响应式断点

| 断点 | 屏幕宽度 | 用途 |
|------|---------|------|
| `sm` | 640px | 内边距调整 `px-4 sm:px-6` |
| `lg` | 1024px | 主要布局切换（单列→双列） |

---

## 特殊组件规范

### 版本号标签

```tsx
<div className="text-base lg:text-lg font-bold text-purple-600 bg-purple-50 inline-block px-3 lg:px-4 py-1 rounded-lg">
  v{version.version}
</div>
```

- 紫色背景 `bg-purple-50`
- 紫色文字 `text-purple-600`
- 粗体 `font-bold`
- 内边距 `px-3 lg:px-4 py-1`

### 产品切换标签

**选中状态**：
```tsx
className="px-2.5 py-1 text-xs font-medium rounded-full bg-gradient-to-r from-purple-600 to-blue-600 text-white shadow-sm"
```

**未选中状态**：
```tsx
className="px-2.5 py-1 text-xs font-medium rounded-full bg-white text-gray-600 hover:bg-purple-50 hover:text-purple-600 border border-gray-200"
```

### 图片灯箱

- 最大高度：`400px`
- 点击放大：全屏灯箱显示
- 悬停效果：`hover:opacity-90`

---

## 空状态设计

### 无更新记录

```tsx
<div className="text-center py-20">
  <div className="inline-block p-6 rounded-full bg-purple-100 mb-4">
    <span className="text-4xl">📭</span>
  </div>
  <p className="text-gray-500 text-lg">暂无更新记录</p>
</div>
```

---

## 页脚规范

```tsx
<footer className="border-t border-purple-100 bg-white/50 mt-20">
  <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8 text-center text-gray-500 text-sm">
    <p>© {new Date().getFullYear()} · {config.site.title}</p>
  </div>
</footer>
```

---

## 实现文件

### 前台展示
- `components/ChangelogClient.tsx` - 主要实现
- `components/VersionCard` - 版本卡片组件
- `components/ListItem` - 列表项组件

### 后台预览
- `components/VersionEditor.tsx` - PreviewCard 组件
- `components/ListItem` - 预览列表项组件

### 通用组件
- `components/ImageLightbox.tsx` - 图片灯箱
- `components/ImagePicker.tsx` - 图片选择器

---

## 更新日志

- **2026-01-20**: 初始设计系统文档
  - 定义字体层级（版本标题、分类标题、一级列表、二级列表）
  - 记录颜色、间距、布局规范
  - 统一前后台展示样式
